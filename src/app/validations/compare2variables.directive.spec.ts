import { Compare2variablesDirective } from './compare2variables.directive';

describe('Compare2variablesDirective', () => {
  it('should create an instance', () => {
    const directive = new Compare2variablesDirective();
    expect(directive).toBeTruthy();
  });
});
